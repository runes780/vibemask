"""VibeMask detection accuracy evaluation harness.

Run with::

    python -m eval.runner --engine hybrid
    python -m eval.runner --engine deterministic
    python -m eval.runner --engine regex --report eval/reports/regex.json
"""
